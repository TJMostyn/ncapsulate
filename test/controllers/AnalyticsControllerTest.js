describe('AnalyticsController', function () {
    
    beforeEach(angular.mock.module('analyticsApp'));
    
    var $controller;

    beforeEach(angular.mock.inject(function(_$controller_){
      $controller = _$controller_;
    }));
});